# TSX generation prototype

This directory is reserved for TSX syntax experiments that generate the proposed `system_*` Circuit JSON elements.

The first target should be generating the same logical diagram as `reference-images/refimage001.json`, then feeding that JSON into the SVG conversion prototype.
